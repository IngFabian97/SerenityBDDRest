# Project Properties
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configureondemand=true

# Build Properties
encoding=UTF-8

# Test Properties
parallel.tests=4

# Environment Properties (Profiles)
# Para usar perfil dev: ./gradlew test -Pprofile=dev
# Para usar perfil prod: ./gradlew test -Pprofile=prod
profile=prod

# Dev profile
restapi.baseurl.dev=http://localhost:5000/api

# Prod profile
restapi.baseurl.prod=https://reqres.in/api

# Active baseurl (se selecciona según el perfil)
restapi.baseurl=https://reqres.in/api
